<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="@drawable/rounded_background">

    <!-- Confirmation message -->
    <TextView
        android:id="@+id/textViewConfirmAddMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:text="Do you want to add this vehicle?"
        android:textColor="#FFFFFF"
        android:textSize="25sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/textViewConfirmVehicleDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="DETAILS:"
        android:textSize="20sp"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:layout_below="@id/textViewConfirmAddMessage"
        android:layout_marginBottom="16dp"/>

    <ImageView
        android:id="@+id/imageViewConfirmVehicleLogo"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_centerHorizontal="true"
        android:scaleType="fitCenter"
        android:layout_below="@id/textViewConfirmVehicleDetails"
        android:layout_marginBottom="16dp"
        android:src="@drawable/default_logo"/>

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/imageViewConfirmVehicleLogo"
        android:orientation="horizontal">

        <!-- Cancel Button -->
        <Button
            android:id="@+id/btnCancelAdd"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Cancel"
            android:textSize="16sp"
            android:textColor="#000000"
            android:background="@drawable/rounded_button_red"
            android:layout_marginEnd="8dp"/>

        <!-- Confirm Button -->
        <Button
            android:id="@+id/btnConfirmAdd"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Add"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:background="@drawable/rounded_button"/>
    </LinearLayout>
</RelativeLayout>
